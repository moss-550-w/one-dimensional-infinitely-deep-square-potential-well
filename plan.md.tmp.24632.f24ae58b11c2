# plan.md — 开发实施计划

> 本文件为「一维无限深方势阱思想之旅」(v2.0) 的工程实施计划，是 `Claude.md`（权威规范）与 `design.md`（设计方案）的落地路线图。任何排期、任务拆分、验收判定均以本文件为准；当与 `Claude.md` 冲突时，以 `Claude.md` 为最高优先级。

---

## 一、总体策略

### 1.1 核心原则

1. **核心优先，卫星渐进**：先打通"核心模拟器 + 状态总线 + 场景管理"主干，再逐章挂载卫星空间。每个卫星空间可独立开发、独立销毁。
2. **数学先行，视觉派生**：每个功能先实现 `QuantumMath` 中可单测的纯函数（本征态、傅里叶系数、内积、坍缩抽样），再驱动视觉层。视觉是数学的派生表现，不允许"看起来对但算错"。
3. **垂直切片交付**：每个里程碑产出一个可在浏览器运行、可手动验收的完整切片，而非横向铺开半成品。
4. **生命周期纪律**：任何 3D 资源从诞生起就配套 `dispose()`，离开即释放，杜绝后期补救。

### 1.2 交付节奏

采用 7 个里程碑（M0–M6）线性推进，每个里程碑对应一个可演示状态。核心模拟器的四个阶段（0→3）与章节解锁严格绑定，禁止跳过前置阶段。

| 里程碑 | 名称 | 对应产物 | 模拟器阶段 |
|--------|------|----------|-----------|
| M0 | 工程地基 | 脚手架 + 基础库 + 空场景 | — |
| M1 | 经典混沌 | 核心模拟器阶段0 + 第一章 | 阶段 0 |
| M2 | 波的语言 | 第二章傅里叶合成器 → 阶段1 | 阶段 0→1 |
| M3 | 无限的几何 | 第三章希尔伯特投影 → 阶段2 | 阶段 1→2 |
| M4 | 量子公理剧场 | 第四章测量/动量 → 阶段3 | 阶段 2→3 |
| M5 | 思想网络 + 结语 | 第五章、第六章 | 阶段 3 |
| M6 | 性能与降级 | Web Worker / 移动端 2.5D / 优化 | 全阶段 |

---

## 二、里程碑详解（WBS）

### M0 — 工程地基

**目标：** 可 `npm run dev` 启动，渲染一个空的深空蓝场景，状态总线与数学库具备最小可测试骨架。

| 任务 | 交付物 | 验收标准 |
|------|--------|----------|
| 初始化 Vite 工程 | `package.json`、`vite.config.js` | `npm run dev` 启动无报错 |
| 安装依赖 | three、mathjs、gsap、tailwindcss | 版本锁定，three ≥ r150 |
| 建立目录结构 | 按 `Claude.md` 5.1 创建 `src/core`、`src/satellites`、`src/shared` | 目录与规范一致 |
| 入口与场景管理器 | `index.html`、`src/main.js` | 渲染深空蓝 `#0f172a` 背景，含 resize 处理 |
| 事件总线 | `src/core/StateBus.js` | PubSub `on/off/emit`，含单测 |
| 数学库骨架 | `src/core/QuantumMath.js` | 导出本征值/本征函数纯函数，含单测 |
| 场景生命周期基类 | `src/shared/utils/SceneBase.js` | 提供 `init/update/dispose` 契约，dispose 释放 geometry/material/texture |

**风险：** Vite + GLSL 导入插件配置。**应对：** M0 即引入 `vite-plugin-glsl`，避免后期返工。

---

### M1 — 经典混沌（模拟器阶段0 + 第一章）

**目标：** 呈现 `design.md` 第一章——半透明盒子内 500 个经典粒子弹性碰撞，点击选项触发粒子逃逸化作文字。

| 任务 | 交付物 | 验收标准 |
|------|--------|----------|
| 势阱盒子模型 | 半透明盒体 + 柔和边框 | 视觉符合玻璃拟态 |
| 粒子系统 | `InstancedMesh`，≤500 实例 | 单一 draw call，发光小球 |
| 经典碰撞 | 与盒壁弹性反射 | 能量守恒，无穿透/无粘壁 |
| 核心模拟器骨架 | `src/core/Simulator.js` | 持有 `currentStage=0`、`unlockedModules[]` |
| 第一章场景 | `src/satellites/Chapter1/` | 中央问题 + 两选项 |
| 逃逸动画 | GSAP 时间线 | 粒子飞向相机化作文字，`power2.out` |

**关键约束：** 粒子运动为牛顿力学随机初值；本阶段**无**任何量子/波函数计算。

---

### M2 — 波的语言（第二章 → 模拟器阶段1）

**目标：** 傅里叶合成器实验室，合成方波并展现吉布斯现象；完成后解锁模拟器阶段1（模式分解器）。

| 任务 | 交付物 | 验收标准 |
|------|--------|----------|
| 傅里叶级数计算 | `QuantumMath.fourierSquareWave(N,x)` | 正弦级数 `Σ 4/(π(2k-1))·sin((2k-1)πx/L)`，单测覆盖 |
| 谐波工作台 UI | ≤20 个谐波发生器，振幅/相位旋钮 | 独立可调，标注"截断" |
| 实时叠加曲线 | 2D Canvas 绘制 → 贴 3D 平面纹理 | 调参实时重绘 |
| 吉布斯过冲高亮 | 跳变处峰值标注 | 数值显示 ≈ 1.08949 倍 |
| 阶段0→1 转场 | GSAP 驱动 | 粒子轨迹消失，浮现波动曲线 |
| 模式分解器面板 | 模拟器侧栏 | 混沌曲线分解为驻波本征态 |
| 解锁逻辑 | `Simulator.unlock('fourier')` | 经 StateBus 同步阶段升级 |

**关键约束（不可妥协）：** 截断必须真实呈现吉布斯过冲，禁止"假装完美逼近"。

---

### M3 — 无限的几何（第三章 → 模拟器阶段2）

**目标：** 希尔伯特空间三维类比投影，向量约束于单位球面，标注"类比"边界；完成后解锁阶段2（几何投影）。

| 任务 | 交付物 | 验收标准 |
|------|--------|----------|
| 永久性标签 | 场景上方"三维类比：函数即向量" | 不可关闭，常驻 |
| 三正交基轴 | `sin(πx/L)`、`sin(2πx/L)`、`sin(3πx/L)` | 标注清晰 |
| 内积计算 | `QuantumMath.innerProduct(m,n)` | `(2/L)∫sin(mπx/L)sin(nπx/L)dx`，正交=0 |
| 向量拖拽 | 球坐标约束算法 | 端点严格锁定单位球面（归一化） |
| 系数联动 | 三分量 ↔ 傅里叶系数 (c1,c2,c3) | 与第二章导入波一致 |
| 高维"折叠"轴 | 第4、5 维以折叠态出现 | 不可展开为清晰欧氏轴，配文说明类比边界 |
| 正交性演示 | 两正弦乘积曲线 + 积分=0 动画 | 数值与动画一致 |
| 阶段1→2 转场 | GSAP | 曲线 → 旋转矢量，投影对应阶段1幅度 |

**关键约束（不可妥协）：** 必须标明"类比"且明确维度限制；拖拽必须满足归一化。

---

### M4 — 量子公理剧场（第四章 → 模拟器阶段3）

**目标：** 整合全部模块，实现制备态、概率性测量坍缩（不可逆）、位置/动量表象切换与不确定性定量展示。

| 任务 | 交付物 | 验收标准 |
|------|--------|----------|
| 态制备 | 拖拽向量 → 波函数曲线 + 概率密度云 | `ψ(x)=Σ c_n ψ_n(x)` 实时更新 |
| 归一化校验 | 制备后 `Σ|c_n|²=1` | 偏差 < 1e-6 |
| 时间演化（可选展示） | `exp(-iE_n t/ħ)` 相位因子 | 干涉正确，制备时 t=0 |
| 测量基选择 | 能量/位置/动量 | UI 可切换 |
| 概率坍缩 | 按 `|c_n|²` 加权随机抽样 | 一次性、不可逆，向量跳至对应轴，波函数变纯 `ψ_n` |
| 重置 | 恢复制备态 | 唯一可逆入口 |
| 动量表象 | 位置波函数数值 FFT | 切换正确显示傅里叶变换 |
| 波包压缩 | 位置压缩 → 动量展宽 | 标准差乘积 `Δx·Δp ≈ ħ/2` 定量显示 |
| 阶段2→3 转场 | GSAP | 完整量子模拟器形态 |

**关键约束（不可妥协）：** 测量必须概率性、一次性、不可逆；本征值公式 `E_n = n²π²ħ²/(2mL²)` 正确。

---

### M5 — 思想网络 + 结语（第五、六章）

**目标：** 3D 力导向知识图谱 + 开放式结语。

| 任务 | 交付物 | 验收标准 |
|------|--------|----------|
| 力导向图引擎 | `src/satellites/Chapter5/` | 节点稳定布局，可拖拽探索 |
| 节点数据 | 毕达哥拉斯…哥德尔等 | 与 `Claude.md` 6.5 节点一致 |
| 连线颜色编码 | 启发蓝/解决绿/挑战红/独立发现橙 | 四类色彩正确 |
| 哥德尔–海森堡 | **无直接连线** | 点击中间空白才浮现"理性边界"反思文字 |
| 第六章场景 | `src/satellites/Chapter6/` | 回到阶段3模拟器 |
| 灰暗禁用按钮 | "同时知道确切位置和动量" | 不可点击 |
| 结语文字 | GSAP 缓入浮现 | 只提问，不给答案，无句号收尾 |

**关键约束：** 哥德尔与海森堡不得直接连线，防止误导性类比。

---

### M6 — 性能与降级

**目标：** 计算密集任务移出主线程，移动端 2.5D 降级，达成最低性能门槛。

| 任务 | 交付物 | 验收标准 |
|------|--------|----------|
| Web Worker | 傅里叶合成、矩阵、坍缩随机数 | `postMessage` + Transferable |
| Shader 化 | 波函数演化/概率密度云 GLSL | GPU 计算，避免 CPU 循环 |
| 性能探测 | `navigator.hardwareConcurrency` + 帧率测试 | 自动分级 |
| 移动端 2.5D | 预渲染序列帧 / Canvas 热点交互 | 保留概念传达 |
| 生命周期审计 | 全卫星 `dispose()` 复查 | 离开无内存泄漏 |
| 性能验收 | 中端机（骁龙7系列等效） | ≥ 30fps |

---

## 三、依赖关系

```
M0 ──► M1 ──► M2 ──► M3 ──► M4 ──► M5
                                    │
M0 ─────────────────────────────► M6（横切，建议 M2 起持续介入）
```

- **强依赖**：模拟器阶段升级严格线性，M(n) 解锁条件依赖 M(n-1) 完成。
- **横切关注点**：M6（性能/Worker/降级）虽列于末位，但 Worker 接口、dispose 契约应自 M0 预留、各里程碑持续遵守，避免末期重构。

---

## 四、贯穿性验收红线（每个里程碑必查）

源自 `Claude.md` 第二节"不可妥协的约束"，任一不达标即里程碑不通过：

1. **概念诚实性**：关键点标注"简化模型/类比"及其边界。
2. **动机内生性**：新工具由前序"描述无力"驱动，禁止"某某出现了"式宣告。
3. **物理/数学准确**：归一化、概率坍缩不可逆、本征值公式、吉布斯现象、希尔伯特投影标注类比。
4. **资源生命周期**：离开卫星空间完全释放 geometry/material/texture/listener。
5. **即时视觉反馈**：每个交互有清晰即时反馈；首次拖拽对象有脉冲/微动效引导。
6. **代码注释**：任何精度简化必须注释说明原因。

---

## 五、风险登记

| 风险 | 等级 | 应对 |
|------|------|------|
| 无限维 → 3D 降维的概念失真 | 高 | 严格执行"类比-局限-超越"，折叠轴 + 永久标签 |
| 移动端 3D 性能不达标 | 高 | M6 提前在 M2 验证 2.5D 降级链路 |
| Web Worker 通信与主线程状态同步复杂 | 中 | StateBus 统一收口，Worker 仅做纯计算 |
| 傅里叶/FFT 数值精度与可视化偏差 | 中 | 纯函数单测先行，视觉派生于数学结果 |
| GSAP 转场与场景销毁竞态 | 中 | 转场完成回调中再执行 dispose |
| 测量坍缩随机性可复现性（调试难） | 低 | Worker 随机数可注入种子，便于回归 |

---

## 六、工程约定

- **目录**：严格遵循 `Claude.md` 5.1；每个卫星章节自带 `index.js`（场景类）+ 局部组件。
- **数学单测**：`QuantumMath` 所有导出函数必须有对应测试（本征值、傅里叶系数、内积、归一化、坍缩分布）。
- **命名**：语义化、见名知意；颜色/字体/物理常数集中常量化，杜绝魔法值。
- **提交粒度**：按里程碑内任务行提交，信息含"做什么 + 为什么"。
- **降级开关**：性能分级结果存入 StateBus，全站可读。

---

## 七、当前状态与下一步

- **现状**：仅有 `Claude.md`、`design.md`、`plan.md`，无代码。
- **下一步（M0 起步）**：初始化 Vite 工程、安装依赖、搭建目录与 `StateBus` / `QuantumMath` / `SceneBase` 骨架。
- **M0 完成判据**：`npm run dev` 渲染空的深空蓝场景，基础库单测通过。

---

*本计划随开发迭代更新；里程碑验收以第四节红线为准，技术权威以 `Claude.md` 为准。*
